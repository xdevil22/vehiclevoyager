export interface VilliersFlight {
  aircraftType?: string;
  origin?: string;
  destination?: string;
  departureDate?: string;
  departureTime?: string;
  price?: string;
  seats?: string;
  bookingLink?: string;
}
